/* 1. 全局基础：消除所有默认间距，确保吸顶前提 */
page {
  background-color: #f5f5f7;
  font-size: 28rpx;
  color: #333;
  width: 100% !important;
  min-height: 100vh;
  margin: 0 !important; /* 清除页面默认margin */
  padding: 0 !important; /* 清除页面默认padding */
  box-sizing: border-box;
}

/* 2. 核心：container强制吸顶+全屏宽度，关键处理padding-top */
.container {
  width: 100% !important; /* 占满屏幕宽度 */
  min-height: 100vh;
  padding-bottom: 60rpx; /* 底部留空防tabbar遮挡 */
  padding-top: 0 !important; /* 强制顶部内边距为0，解决吸顶问题（核心） */
  margin: 0 !important; /* 清除容器默认margin */
  box-sizing: border-box;
  position: relative;
}

/* 3. 设置模块：全屏宽度，仅保留垂直间距 */
.settings-section {
  width: 100% !important; /* 占满屏幕宽度 */
  background-color: #fff;
  margin-top: 20rpx; /* 模块间垂直间距，无左右margin */
  margin-left: 0 !important;
  margin-right: 0 !important;
  box-sizing: border-box;
}

/* 4. 列表项：全屏宽度+合理点击区域，内容不贴边 */
.settings-item {
  width: 100% !important; /* 占满模块宽度 */
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 100rpx; /* 足够点击高度，符合小程序交互规范 */
  padding: 0 30rpx; /* 内容与屏幕边缘留30rpx间距，不贴边 */
  border-bottom: 1rpx solid #f5f5f5; /* 分割线全屏对齐 */
  margin: 0 !important;
  box-sizing: border-box;
}
/* 最后一个列表项：移除底部分割线，避免多余线条 */
.settings-item:last-child {
  border-bottom: none;
}

/* 5. 列表项左侧：图标+文字排版 */
.settings-left {
  display: flex;
  align-items: center;
}
.settings-icon {
  width: 40rpx;
  height: 40rpx;
  object-fit: contain;
  margin-right: 20rpx; /* 图标与文字间距，提升可读性 */
}
.settings-text {
  font-size: 28rpx;
  color: #333;
}

/* 6. 列表项右侧：箭头图标统一样式 */
.settings-arrow {
  width: 20rpx;
  height: 36rpx;
  object-fit: contain;
  opacity: 0.6; /* 弱化箭头，突出文字主体 */
}

/* 7. 特殊项：高亮警示高危操作 */
/* 退出登录：默认文字色 */
.logout-item .settings-text {
  color: #333;
}
/* 注销账号：红色警示，提醒用户谨慎操作 */
.delete-item .settings-text {
  color: #e63946 !important; /* 强制红色，避免被其他样式覆盖 */
}

/* 8. 版本信息：全屏宽度+居中对齐，与模块风格统一 */
.version-info {
  width: 100% !important; /* 占满屏幕宽度 */
  margin-top: 20rpx;
  padding: 25rpx 0; /* 上下内边距，垂直居中 */
  text-align: center;
  margin-left: 0 !important;
  margin-right: 0 !important;
  box-sizing: border-box;
}
.version-text {
  font-size: 24rpx;
  color: #999;
}